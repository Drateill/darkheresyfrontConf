# Secrets de l'application — repo GitHub privé, ne pas partager publiquement.
# Pour encoder une valeur : echo -n 'ma_valeur' | base64
---
# Credentials MongoDB pour le pod mongo in-cluster
apiVersion: v1
kind: Secret
metadata:
  name: mongo-creds
  namespace: darkheresy
type: Opaque
data:
  username: ZHJhdGVpbA==
  password: TTR4M25jMy81OQ==
---
# Credentials MongoDB pour l'API backend
apiVersion: v1
kind: Secret
metadata:
  name: darkheresy-api-secret
  namespace: darkheresy
type: Opaque
data:
  # echo -n 'votre_username' | base64
  DB_USER: REPLACE_WITH_BASE64_VALUE
  # echo -n 'votre_password' | base64
  DB_PWD: REPLACE_WITH_BASE64_VALUE
  # MongoDB Atlas : echo -n '@cluster.mongodb.net/' | base64
  # In-cluster     : echo -n 'mongo-service'        | base64
  DB_URI: REPLACE_WITH_BASE64_VALUE
