apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myingress
  labels:
    name: myingress
spec:
  rules:
  - host: darkheresyfront
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: darkheresyfront-service
            port: 
              number: 3000
